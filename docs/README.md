# docs/ — the specifying briefs

Exported 2026-09-22 from the project vault (`gaku-se498`, private). The vault is the source of truth; re-export when a brief changes.

| File | What |
|---|---|
| `capture-app-build-scaffold.md` / `.pdf` | the iOS app build plan: modules, clock, verified Polar SDK 8.3.0 surface, build order, ten acceptance tests |
| `data-management-plan.md` / `.pdf` | session bundle, storage tiers, speech review, what leaves, retention, this repo's layout |
| `lab-session-runbook.md` / `.pdf` | the 65-minute session step by step, stop rules, same-day QA |
| `pipeline-input-to-verdict.md` / `.pdf` | every stage mic → zone with its silent failure |

Markdown copies have vault wikilinks flattened to plain names.
